{
  "name": "@webdb/core",
  "version": "0.1.0",
  "type": "module",
  "description": "Ultra-lean browser-native relational database engine",
  "scripts": {
    "generate:layouts": "node scripts/generate_layouts.js",
    "prebuild": "node scripts/generate_layouts.js",
    "build": "tsc",
    "pretest": "node scripts/generate_layouts.js",
    "test": "vitest run",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "demo": "tsc && node dist/examples/demo.js",
    "playground": "node scripts/serve.js",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "eslint": "^10.11.0",
    "fake-indexeddb": "^6.0.0",
    "markdown-it-mathjax3": "^4.3.2",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.70.1",
    "vitepress": "^1.5.0",
    "vitest": "^3.0.0"
  }
}
